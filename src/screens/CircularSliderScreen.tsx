import React from 'react';
import { CircularSlider } from '../components/circuler-slider';

/**
 * CircularSliderScreen - Wrapper screen for the CircularSlider component
 * This screen simply renders the CircularSlider component which displays
 * an interactive circular image carousel with animated transitions
 */
export const CircularSliderScreen = (): React.JSX.Element => {
  return <CircularSlider />;
};
